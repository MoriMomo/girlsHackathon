# Optional: enable the real AI receipt scan (Google Gemini vision).
# Get a FREE key at https://aistudio.google.com  ->  "Get API key".
# Copy this file to ".env" (which is git-ignored) and paste your key below.
#
# WARNING: a key in a frontend build is visible to anyone who inspects the page.
# For the hackathon demo use a free Gemini key and delete/rotate it afterward.
# Never put a billing-enabled key in a public site.
VITE_GEMINI_API_KEY=
